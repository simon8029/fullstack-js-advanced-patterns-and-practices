
import React from 'react';
import { Container, Typography } from '@mui/material';

const Firebase = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Firebase for Fullstack Applications
      </Typography>
      <Typography variant="body1">
        Topics: Firebase Authentication, Firestore, Realtime Database, Cloud Functions, Hosting.
      </Typography>
    </Container>
  );
};

export default Firebase;
